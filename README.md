<div align="center">
  <h1>Hatani–TikTok: Ультра-быстрый Telegram бот</h1>
  <p>
    <strong>Ультимативное решение для скачивания видео и альбомов из TikTok и Douyin без водяных знаков, с полной статистикой и мгновенной отправкой.</strong>
  </p>
</div>

---

Этот Telegram-бот — результат полной переработки и улучшения, нацеленный на максимальную скорость и информативность. Он использует современный асинхронный API для прямого взаимодействия с серверами TikTok/Douyin, обеспечивая молниеносную работу и получение исчерпывающих данных о контенте.

## ✨ Ключевые особенности

-   🚀 **Мгновенная Отправка**: Пользователь получает видео или альбом за секунды. Бот использует трехэтапную отправку: сначала медиа, затем подгрузка и обновление полной информации.
-   📊 **Исчерпывающая Статистика**: Вся доступная информация о видео и его авторе:
    -   **Видео**: Лайки, просмотры, комментарии, репосты, сохранения.
    -   **Автор**: Количество подписчиков и общее число лайков на всех видео.
-   🛠️ **Технические Детали**: Бот скачивает видео в фоновом режиме на сервер для анализа и отображает разрешение, FPS и размер файла.
-   🌐 **Поддержка Douyin & TikTok**: Гибридный API автоматически распознает тип ссылки (глобальный TikTok или китайский Douyin) и обрабатывает ее.
-   ⚡ **Умное Кэширование**: Повторные запросы на одну и ту же ссылку обрабатываются мгновенно, отправляя уже готовый результат из кэша.
-   🎨 **Стильный Дизайн Подписи**: Уникальный и минималистичный формат вывода информации, разработанный специально для этого проекта.
-   🐳 **Развертывание в один клик**: Проект полностью упакован в Docker и Docker Compose для максимально простого и надежного запуска.

---

## 🧠 Архитектура проекта

Проект состоит из двух независимых, но связанных сервисов, работающих в Docker-контейнерах:

1.  **`api` (Python, FastAPI)**
    -   **Основа**: Модифицированная версия `Douyin_TikTok_Download_API`.
    -   **Задачи**:
        -   Напрямую общается с серверами TikTok и Douyin.
        -   Генерирует все необходимые подписи (`X-Bogus`, `A-Bogus`) для обхода защиты.
        -   Предоставляет боту простые эндпоинты для получения данных.
        -   Анализирует видеофайлы с помощью `ffmpeg` для извлечения технических деталей (FPS, размер).

2.  **`bot` (Node.js, Telegram API)**
    -   **Задачи**:
        -   Общается с пользователем в Telegram.
        -   Оркестрирует весь процесс: получает ссылку, делает запросы к нашему `api` сервису, формирует и отправляет сообщения.
        -   Реализует сложную трехэтапную логику отправки и редактирования сообщений.

Такое разделение позволяет независимо обновлять и масштабировать API и бота, а также обеспечивает высокую производительность и стабильность.

## 🛠️ Технологический стек

-   **Бот**: `Node.js`, `node-telegram-bot-api`, `axios`
-   **API**: `Python`, `FastAPI`, `uvicorn`, `ffmpeg`
-   **Окружение**: `Docker`, `Docker Compose`

---

## 🚀 Запуск через Docker (Единственный и рекомендуемый способ)

Проект полностью настроен для запуска с помощью Docker и Docker Compose. Это самый простой и надежный способ.

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Rewixx-png/Hatani-TikTok.git
cd Hatani-TikTok
```

### Шаг 2: Настройка переменных окружения

Вам нужно создать один конфигурационный файл `bot/.env` на основе примера.

1.  **Создайте файл `.env`:**
    ```bash
    cp bot/.env.example bot/.env
    ```

2.  **Настройте его:**
    Откройте файл `bot/.env` и вставьте токен вашего Telegram-бота. **Остальные переменные трогать не нужно**, они уже настроены для работы внутри Docker.
    ```env
    TELEGRAM_BOT_TOKEN="12345:ABC-DeFGh..."
    TIKTOK_API_URL="http://api:80" 
    PUBLIC_SERVER_URL="http://YOUR_SERVER_IP:8000"
    ```

### Шаг 3: Настройка Cookie для API (Критически важно!)

Для стабильной работы API требуются актуальные `Cookie` от TikTok.

1.  **Откройте файл конфигурации API:**
    `douyin-api/crawlers/tiktok/web/config.yaml`

2.  **Получите `Cookie`:**
    -   Откройте `tiktok.com` в браузере.
    -   Откройте Инструменты разработчика (F12).
    -   Перейдите на вкладку "Network" (Сеть).
    -   Обновите страницу.
    -   Найдите любой запрос к `tiktok.com`, кликните на него.
    -   В разделе "Request Headers" (Заголовки запроса) найдите заголовок `cookie`, кликните на него правой кнопкой мыши и выберите "Copy value".

3.  **Вставьте `Cookie` в конфиг:**
    Замените всю длинную строку в поле `Cookie:` на ту, что вы скопировали.

    ```yaml
    # douyin-api/crawlers/tiktok/web/config.yaml

    TokenManager:
      tiktok:
        headers:
          User-Agent: ...
          Referer: https://www.tiktok.com/
          # ВОТ СЮДА ВСТАВЛЯТЬ СКОПИРОВАННЫЕ COOKIE
          Cookie: ttwid=...; msToken=...; и так далее...
    ```

### Шаг 4: Запуск проекта

Выполните одну команду из корня проекта:

```bash
docker compose up -d --build
```

Docker автоматически скачает все нужные образы (включая Python, Node.js и **ffmpeg**), установит все зависимости и запустит оба сервиса. Первая сборка может занять несколько минут.

---

### 📦 Управление контейнерами

-   **Посмотреть логи (очень полезно для отладки):**
    ```bash
    # Логи API (Python)
    docker compose logs -f api
    
    # Логи бота (Node.js)
    docker compose logs -f bot
    ```

-   **Остановить проект:**
    ```bash
    docker compose down
    ```

-   **Пересобрать и запустить (если вы внесли изменения в код):**
    ```bash
    docker compose up -d --build
    ```

-   **Проверить запущенные контейнеры проекта:**
    ```bash
    docker compose ps
    ```

---

## ⚖️ Лицензия и использование

Этот проект был создан с любовью командой **Hatani Squad** для личного использования и образовательных целей. Основа API (`Douyin_TikTok_Download_API`) распространяется по лицензии Apache 2.0.

**Важно:** Бот не предназначен для коммерческого использования. Любое извлечение выгоды с его помощью — на ваш страх и риск. Мы не несем ответственности за возможную блокировку ваших аккаунтов или другие последствия. Используйте с умом.