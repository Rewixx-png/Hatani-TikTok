# Telegram Бот для Скачивания TikTok Видео и Альбомов с Полной Статистикой

Этот Telegram-бот — ваш универсальный инструмент для сохранения контента из TikTok. Он позволяет скачивать как видео, так и фото-альбомы без водяных знаков, получать о них исчерпывающую информацию и реальную статистику (лайки, просмотры, комментарии, репосты).

## ✨ Возможности

-   📥 **Скачивание без водяного знака**: Видео и фото-альбомы в оригинальном качестве.
-   🐳 **Развертывание в один клик**: Проект полностью упакован в Docker.
-   📊 **Полная и реальная статистика**: Лайки, просмотры, комментарии, репосты и сохранения.
-   🎵 **Распознавание и скачивание музыки**: Интеграция с Shazam для определения трека в видео и кнопка для его скачивания.
-   ℹ️ **Подробная информация**: Автор, дата создания и описание.
-   🛡️ **Гибридная и надежная архитектура**: Бот использует разные подходы для видео и фото.
-   🔄 **Автоматическая перезагрузка**: API-сервис перезапускается каждые 12 часов для обновления сессий и стабильной работы.

---

## 🚀 Запуск через Docker (Рекомендуется)

Проект полностью настроен для запуска с помощью Docker и Docker Compose. Это самый простой и надежный способ.

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Rewixx-png/advanced-tiktok-downloader.git
cd advanced-tiktok-downloader
```

### Шаг 2: Настройка переменных окружения

Вам нужно создать два конфигурационных файла на основе примеров.

1.  **Настройка API:**
    -   Скопируйте пример: `cp python_api/.env.example python_api/.env`
    -   Откройте файл `python_api/.env` и вставьте ваш `ms_token`.
    > **Как получить `ms_token`:**
    > 1. Откройте `tiktok.com` в браузере.
    > 2. Откройте Инструменты разработчика (F12).
    > 3. Вкладка "Application" (Firefox: "Хранилище") -> "Cookies" -> "https://www.tiktok.com".
    > 4. Найдите и скопируйте значение `ms_token`.

2.  **Настройка Бота:**
    -   Скопируйте пример: `cp nodejs_bot/.env.example nodejs_bot/.env`
    -   Откройте файл `nodejs_bot/.env` и вставьте токен вашего Telegram-бота, а также публичный IP-адрес вашего сервера.

### Шаг 3: Запуск проекта

Выполните одну команду из корня проекта:

```bash
docker-compose up -d --build
```

Docker автоматически скачает нужные образы, установит все зависимости (включая браузер для Python API) и запустит оба сервиса.

---

### 📦 Управление контейнерами

-   **Посмотреть логи (очень полезно для отладки):**
    ```bash
    # Логи API
    docker-compose logs -f api
    
    # Логи бота
    docker-compose logs -f bot
    ```

-   **Остановить проект:**
    ```bash
    docker-compose down
    ```

-   **Пересобрать и запустить (если вы внесли изменения в код):**
    ```bash
    docker-compose up -d --build
    ```